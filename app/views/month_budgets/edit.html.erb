<div class="budget-edit">
  <h2>Edit Month Budget</h2>
  <%= form_for [@user, @month_budget] do |form| %>
    <div class="form-field">
      <%= form.text_field :target_amount %><h4>Edit Amount</h4>
    </div>
    <%= form.submit "Change Budget" %>
  <% end %>
</div>