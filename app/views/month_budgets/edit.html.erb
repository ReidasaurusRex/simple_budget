<div class="userBudgetForm">
  <h2>Edit Month Budget</h2>
  <%= form_for [@user, @month_budget] do |form| %>
    <div class="form-field">
      <p><%= form.text_field :target_amount %><br><span>Edit Amount</span></p>
    </div>
    <p><%= form.submit "Change Budget" %></p>
  <% end %>
</div>