<div class="budget-edit">
  <h1>Edit Month Budget</h1>
  <%= form_for [@user, @month_budget] do |form| %>
    <div class="form-field">
      <%= form.text_field :target_amount %><h4>New Amount</h4>
    </div>
    <%= form.submit "Change Budget" %>
  <% end %>
</div>