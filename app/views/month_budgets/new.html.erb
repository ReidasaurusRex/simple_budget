<div class="budget-new">
  <h1>New Month Budget</h1>
  <%= form_for [@user, MonthBudget.new] do |form|  %>
    <div class="form-field">
      <%= form.text_field :target_amount %><h4>Target Budget Amount</h4>
    </div>
    <%= form.submit "Create Budget", class: 'wide-submit' %>
  <% end %>
</div>