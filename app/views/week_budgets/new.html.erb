<div class="budget-new">
  <h1>Create Week Budget</h1>
  <%= form_for [@user, WeekBudget.new] do |form| %>
    <div class="form-field">
      <%= form.text_field :target_amount %><h4>Target Budget Amount</h4>
    </div>
    <%= form.submit "Create Budget" %>
  <% end %>
</div>